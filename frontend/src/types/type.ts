export interface Note {
  _id: string;
  title: string;
  content: string;
  category: string;
  createdAt: string;
  updatedAt: string;
}

export interface user {
  // _id: string;
  name: string;
  email: string;
  password: string;
  pic: string;
  // isAdmin: boolean;
  // token: string;
}
